% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class.R
\name{createScMetaLink}
\alias{createScMetaLink}
\title{Create scMetaLink Object}
\usage{
createScMetaLink(
  expression_data,
  cell_meta,
  cell_type_column = "cell_type",
  min_cells = 10,
  min_genes = 200
)
}
\arguments{
\item{expression_data}{A matrix or dgCMatrix of normalized expression values (genes x cells)}

\item{cell_meta}{A data.frame containing cell metadata}

\item{cell_type_column}{Character. Column name in cell_meta containing cell type annotations}

\item{min_cells}{Integer. Minimum number of cells per cell type (default: 10)}

\item{min_genes}{Integer. Minimum number of genes detected per cell (default: 200)}
}
\value{
A scMetaLink object
}
\description{
Initialize a scMetaLink object from expression data and cell metadata
}
\examples{
\donttest{
# Create from expression matrix
expr_mat <- matrix(rpois(1000, 5), nrow = 100, ncol = 10)
rownames(expr_mat) <- paste0("Gene", 1:100)
colnames(expr_mat) <- paste0("Cell", 1:10)
meta <- data.frame(cell_type = rep(c("TypeA", "TypeB"), each = 5))
rownames(meta) <- colnames(expr_mat)
obj <- createScMetaLink(expr_mat, meta, "cell_type")
}
}
