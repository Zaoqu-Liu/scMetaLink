% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class.R
\name{createScMetaLinkFromSCE}
\alias{createScMetaLinkFromSCE}
\title{Create scMetaLink from SingleCellExperiment}
\usage{
createScMetaLinkFromSCE(
  sce,
  cell_type_column = "cell_type",
  assay_name = "logcounts",
  min_cells = 10
)
}
\arguments{
\item{sce}{A SingleCellExperiment object}

\item{cell_type_column}{Character. Column name in colData for cell type annotations}

\item{assay_name}{Character. Name of assay to use (default: "logcounts")}

\item{min_cells}{Integer. Minimum cells per cell type}
}
\value{
A scMetaLink object
}
\description{
Initialize a scMetaLink object from a SingleCellExperiment object
}
\examples{
\donttest{
if (requireNamespace("SingleCellExperiment", quietly = TRUE)) {
  library(SingleCellExperiment)
  # Create example SCE
  counts <- matrix(rpois(1000, 5), nrow = 100, ncol = 10)
  rownames(counts) <- paste0("Gene", 1:100)
  colnames(counts) <- paste0("Cell", 1:10)
  sce <- SingleCellExperiment(assays = list(counts = counts, logcounts = log1p(counts)))
  colData(sce)$cell_type <- rep(c("TypeA", "TypeB"), each = 5)
  # obj <- createScMetaLinkFromSCE(sce, "cell_type")
}
}
}
