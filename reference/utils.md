# scMetaLink Utility Functions

Internal utility functions for scMetaLink package

## Usage

``` r
.check_dependencies(packages, type = "error")
```

## Arguments

- packages:

  Character vector of package names

- type:

  Character. "error" to stop, "warning" to warn
